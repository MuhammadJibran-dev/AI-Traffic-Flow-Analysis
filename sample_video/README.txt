## Sample Video

A sample traffic video may be included in the repository for testing purposes.

If the sample video is unavailable, corrupted, or does not play correctly on your system, you can use any traffic surveillance or road traffic video in MP4 format.

Simply update the video path in the notebook:

```python
cap = cv2.VideoCapture("your_video.mp4")
```

and run the notebook again.

